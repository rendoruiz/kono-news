export const StoryList = ({ type }) => {

  return (
    <>
      <ul>

      </ul>
      <button>
        Load More Stories
      </button>
    </>
  );
}

const StoryItem = ({ story, isSelected }) => {

  return (
    <li>

    </li>
  );
}